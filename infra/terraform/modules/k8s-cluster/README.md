# `k8s-cluster` module (placeholder)

Provisions one region's Kubernetes cluster (ARCH-006 §1) plus its Kafka
(ADR-016) and per-service Postgres (ADR-015) infrastructure. Empty until
a cloud provider is chosen (see `infra/terraform/README.md`).
