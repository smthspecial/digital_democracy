# `network` module (placeholder)

Provisions the VPC/networking for one region of ARCH-006's topology.
Empty until a cloud provider is chosen (see `infra/terraform/README.md`) --
add the provider-specific resources here; the module's expected
inputs/outputs (`region` in, `vpc_id` out) are already referenced by
`infra/terraform/main.tf`.
