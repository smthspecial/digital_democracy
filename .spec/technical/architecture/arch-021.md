---
id: ARCH-021
type: arch
title: "System integrity & anti-capture: audit log, multi-approval & rotation"
status: deprecated
linkedIds: TP-013
created: 2026-08-27
---

## Overview

This doc's content (test scenarios, fixtures, edge cases, and traceability) has moved to [TP-013](../test-plans/tp-013.md), a `test-plan`-type document — per this project's convention, test-plan content lives under `test-plan`, not `arch`. This id is retained, deprecated, so nothing that already links to `ARCH-021` breaks; see TP-013 for the current, maintained content.
