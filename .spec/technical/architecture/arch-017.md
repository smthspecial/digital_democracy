---
id: ARCH-017
type: arch
title: "Implementation & public oversight: projects, milestones & outcomes"
status: deprecated
linkedIds: TP-009
created: 2026-08-27
---

## Overview

This doc's content (test scenarios, fixtures, edge cases, and traceability) has moved to [TP-009](../test-plans/tp-009.md), a `test-plan`-type document — per this project's convention, test-plan content lives under `test-plan`, not `arch`. This id is retained, deprecated, so nothing that already links to `ARCH-017` breaks; see TP-009 for the current, maintained content.
