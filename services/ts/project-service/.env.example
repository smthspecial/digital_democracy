PORT=4010

# Optional: real cross-service wiring for ProposalAuthorLookup/ReputationEmitter/LedgerRecorder.
# Unset -> all fall back to no-op defaults so the service still runs standalone.
PROPOSAL_SERVICE_URL=http://localhost:4004
REPUTATION_SERVICE_URL=http://localhost:4011
BUDGET_SERVICE_URL=http://localhost:4007

# Optional: real queue wiring for AuditEmitter via the audit.append queue (ADR-023).
# Unset -> falls back to the no-op default so the service still runs standalone.
NATS_URL=nats://localhost:4222
