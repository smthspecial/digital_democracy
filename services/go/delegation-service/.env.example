PORT=5002

# Optional: real queue wiring for AuditEmitter via the audit.append queue
# (ADR-023). This is the only AuditEmitter wiring this service has. Unset ->
# falls back to the no-op default.
NATS_URL=nats://localhost:4222
